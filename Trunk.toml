[build]
# Build Tailwind CSS before Trunk processes the HTML
# Uses dev build (faster, no minification) for development
# For production: run 'npm run build:prod' before 'trunk build --release'
hooks = [
  { stage = "pre_build", command = "sh", args = ["-c", "mkdir -p dist && npm run build:dev"] }
]

